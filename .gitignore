node_modules
.next
.vercel
.env
.env.local
.env.*.local
.DS_Store
dist
build
coverage
supabase/.temp/

# TypeScript incremental build cache
*.tsbuildinfo

# E2E harness — run-local and SENSITIVE.
# Holds the run manifest (synthetic passwords), captured storage state (live
# session tokens), traces and reports. Never commit any of it.
apps/web/e2e/.run/
playwright-report/
test-results/
.env.e2e.local