import type { GlobalConceptVersion, TenantCompetencyMapping } from "../types"
export function GlobalCompetencyMappingPanel({concepts,mappings}:{concepts:readonly GlobalConceptVersion[];mappings:readonly TenantCompetencyMapping[]}){return <section className="space-y-3 rounded-lg border p-4"><h2 className="text-lg font-semibold">Mapeamento de competências globais</h2><p className="text-sm text-muted-foreground">Mappings são decisões humanas da empresa. Sugestões não são confirmadas automaticamente.</p><div className="text-sm">{concepts.length===0?<p>Nenhum conceito global publicado.</p>:concepts.map((concept)=><div className="flex justify-between border-t py-2" key={concept.id}><span>{concept.name} · v{concept.versionNumber}</span><span>{mappings.find((m)=>m.conceptVersionId===concept.id)?.status??"sem mapping"}</span></div>)}</div></section>}
